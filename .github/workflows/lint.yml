name: lint

# markdownlint with the platform's rules. The job is quartz-book's book-lint.yml
# (BOOK-ONE-TO-QUARTZ §8 step 14).
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  markdownlint:
    uses: textbookproject2026-alt/quartz-book/.github/workflows/book-lint.yml@stable
